# Security Audit Report

**Project:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow`
**Date:** 2026-03-22 23:54:36 UTC
**Risk Score:** 71/100 (HIGH)
**Languages:** python

## Executive Summary

This audit scanned 260 files (98,792 lines of code) across python. Phase 1 discovered 25 potential issues. After data flow tracing, 0 were CONFIRMED exploitable and 7 are LIKELY exploitable. 7 findings were validated with CVSS scores (0 critical, 6 high, 1 medium, 0 low). The overall risk score is **71/100 (HIGH)**.

## Findings Summary

| # | Severity | CVSS | Category | Reachability | File:Line | Status |
|---|----------|------|----------|--------------|-----------|--------|
| 1 | High | 8.9 | Remote Code Execution | LIKELY | `test_placement_visual.py:41` | NEEDS REVIEW |
| 2 | High | 7.4 | SQL Injection | LIKELY | `design_state.py:86` | AUTO-FIX |
| 3 | High | 7.4 | SQL Injection | LIKELY | `agents_cmd.py:201` | AUTO-FIX |
| 4 | High | 7.4 | SQL Injection | LIKELY | `agents_cmd.py:295` | AUTO-FIX |
| 5 | High | 7.4 | SQL Injection | LIKELY | `jlcpcb_db.py:260` | AUTO-FIX |
| 6 | High | 7.4 | SQL Injection | LIKELY | `jlcpcb_db.py:292` | AUTO-FIX |
| 7 | Medium | 4.7 | Weak Cryptography | LIKELY | `test_sexp.py:262` | AUTO-FIX |

## Detailed Findings

### Finding 1: Code Execution

- **Severity:** High (CVSS 8.9)
- **Category:** Remote Code Execution
- **CWE:** CWE-94
- **Reachability:** LIKELY
- **File:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/tests/integration/test_placement_visual.py:41`
- **Language:** python

**What the code does:**
```
exec(compile(source, str(build_path), "exec"), mod.__dict__)
```

**Vulnerability:**
An attacker could execute arbitrary commands on the server by exploiting code_execution in test_placement_visual.py:41. The vulnerable code (exec(compile(source, str(build_path), "exec"), mod.__dict__)) accepts input that reaches a command execution sink without proper sanitization.

**Code Path:**
`/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/tests/integration/test_placement_visual.py:41`

**Proof of Concept:**
An attacker could send input containing shell metacharacters (e.g., ; id or $(whoami)) to the parameter that reaches test_placement_visual.py:41 to execute arbitrary commands.

**Recommended Fix (ASK):**

Before:
```
exec(compile(source, str(build_path), "exec"), mod.__dict__)
```

After:
```
# TODO: Fix code_execution — Avoid eval/exec; use safe alternatives (AST, JSON.parse, etc.)
```

**Why this fix works:** Avoid eval/exec; use safe alternatives (AST, JSON.parse, etc.)

**Regression test:**
```python
def test_code_execution_fixed():
    # Verify the code_execution vulnerability is fixed
    # Add specific test for /Users/johnbetts/Dropbox/Source/kicad-ai-workflow/tests/integration/test_placement_visual.py:41
    pass  # TODO: implement regression test

```

**CVSS Vector:**
AV:N/AC:H/PR:N/UI:N/S:C/C:H/I:H/A:H

---

### Finding 2: Sql Injection

- **Severity:** High (CVSS 7.4)
- **Category:** SQL Injection
- **CWE:** CWE-89
- **Reachability:** LIKELY
- **File:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/design_state.py:86`
- **Language:** python

**What the code does:**
```
f"**Updated**: {_now_stamp()}",
```

**Vulnerability:**
An attacker could extract, modify, or delete database contents by injecting SQL through design_state.py:86. The code constructs a SQL query using string interpolation/concatenation instead of parameterized queries.

**Code Path:**
`/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/design_state.py:86`

**Proof of Concept:**
An attacker could send a request with a payload like ' OR '1'='1' -- in a parameter that reaches design_state.py:86 to extract unauthorized data from the database.

**Recommended Fix (AUTO_FIX):**

Before:
```
f"**Updated**: {_now_stamp()}",
```

After:
```
cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
```

**Why this fix works:** Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it impossible to inject SQL through parameters.

**Regression test:**
```python
def test_sql_injection_prevented():
    # Attempt SQL injection payload
    malicious_input = "' OR '1'='1' --"
    # Verify the query uses parameterized execution
    # and the payload is treated as a literal string value
    result = query_function(malicious_input)
    assert result is None or len(result) == 0

```

**CVSS Vector:**
AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N

---

### Finding 3: Sql Injection

- **Severity:** High (CVSS 7.4)
- **Category:** SQL Injection
- **CWE:** CWE-89
- **Reachability:** LIKELY
- **File:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/cli/agents_cmd.py:201`
- **Language:** python

**What the code does:**
```
print(f"  Updated: {status.updated_at}")
```

**Vulnerability:**
An attacker could extract, modify, or delete database contents by injecting SQL through agents_cmd.py:201. The code constructs a SQL query using string interpolation/concatenation instead of parameterized queries.

**Code Path:**
`/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/cli/agents_cmd.py:201`

**Proof of Concept:**
An attacker could send a request with a payload like ' OR '1'='1' -- in a parameter that reaches agents_cmd.py:201 to extract unauthorized data from the database.

**Recommended Fix (AUTO_FIX):**

Before:
```
print(f"  Updated: {status.updated_at}")
```

After:
```
cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
```

**Why this fix works:** Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it impossible to inject SQL through parameters.

**Regression test:**
```python
def test_sql_injection_prevented():
    # Attempt SQL injection payload
    malicious_input = "' OR '1'='1' --"
    # Verify the query uses parameterized execution
    # and the payload is treated as a literal string value
    result = query_function(malicious_input)
    assert result is None or len(result) == 0

```

**CVSS Vector:**
AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N

---

### Finding 4: Sql Injection

- **Severity:** High (CVSS 7.4)
- **Category:** SQL Injection
- **CWE:** CWE-89
- **Reachability:** LIKELY
- **File:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/cli/agents_cmd.py:295`
- **Language:** python

**What the code does:**
```
print(f"Pipeline version updated: {version.git_hash[:8]}")
```

**Vulnerability:**
An attacker could extract, modify, or delete database contents by injecting SQL through agents_cmd.py:295. The code constructs a SQL query using string interpolation/concatenation instead of parameterized queries.

**Code Path:**
`/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/cli/agents_cmd.py:295`

**Proof of Concept:**
An attacker could send a request with a payload like ' OR '1'='1' -- in a parameter that reaches agents_cmd.py:295 to extract unauthorized data from the database.

**Recommended Fix (AUTO_FIX):**

Before:
```
print(f"Pipeline version updated: {version.git_hash[:8]}")
```

After:
```
cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
```

**Why this fix works:** Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it impossible to inject SQL through parameters.

**Regression test:**
```python
def test_sql_injection_prevented():
    # Attempt SQL injection payload
    malicious_input = "' OR '1'='1' --"
    # Verify the query uses parameterized execution
    # and the payload is treated as a literal string value
    result = query_function(malicious_input)
    assert result is None or len(result) == 0

```

**CVSS Vector:**
AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N

---

### Finding 5: Sql Injection

- **Severity:** High (CVSS 7.4)
- **Category:** SQL Injection
- **CWE:** CWE-89
- **Reachability:** LIKELY
- **File:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/parts/jlcpcb_db.py:260`
- **Language:** python

**What the code does:**
```
sql = f"SELECT {_SELECT_COLS} FROM parts WHERE parts MATCH ?"
```

**Vulnerability:**
An attacker could extract, modify, or delete database contents by injecting SQL through jlcpcb_db.py:260. The code constructs a SQL query using string interpolation/concatenation instead of parameterized queries.

**Code Path:**
`/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/parts/jlcpcb_db.py:260`

**Proof of Concept:**
An attacker could send a request with a payload like ' OR '1'='1' -- in a parameter that reaches jlcpcb_db.py:260 to extract unauthorized data from the database.

**Recommended Fix (AUTO_FIX):**

Before:
```
sql = f"SELECT {_SELECT_COLS} FROM parts WHERE parts MATCH ?"
```

After:
```
cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
```

**Why this fix works:** Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it impossible to inject SQL through parameters.

**Regression test:**
```python
def test_sql_injection_prevented():
    # Attempt SQL injection payload
    malicious_input = "' OR '1'='1' --"
    # Verify the query uses parameterized execution
    # and the payload is treated as a literal string value
    result = query_function(malicious_input)
    assert result is None or len(result) == 0

```

**CVSS Vector:**
AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N

---

### Finding 6: Sql Injection

- **Severity:** High (CVSS 7.4)
- **Category:** SQL Injection
- **CWE:** CWE-89
- **Reachability:** LIKELY
- **File:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/parts/jlcpcb_db.py:292`
- **Language:** python

**What the code does:**
```
f"SELECT {_SELECT_COLS} FROM parts "
```

**Vulnerability:**
An attacker could extract, modify, or delete database contents by injecting SQL through jlcpcb_db.py:292. The code constructs a SQL query using string interpolation/concatenation instead of parameterized queries.

**Code Path:**
`/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/src/kicad_pipeline/parts/jlcpcb_db.py:292`

**Proof of Concept:**
An attacker could send a request with a payload like ' OR '1'='1' -- in a parameter that reaches jlcpcb_db.py:292 to extract unauthorized data from the database.

**Recommended Fix (AUTO_FIX):**

Before:
```
f"SELECT {_SELECT_COLS} FROM parts "
```

After:
```
cursor.execute("SELECT * FROM users WHERE id = %s", (user_id,))
```

**Why this fix works:** Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it impossible to inject SQL through parameters.

**Regression test:**
```python
def test_sql_injection_prevented():
    # Attempt SQL injection payload
    malicious_input = "' OR '1'='1' --"
    # Verify the query uses parameterized execution
    # and the payload is treated as a literal string value
    result = query_function(malicious_input)
    assert result is None or len(result) == 0

```

**CVSS Vector:**
AV:N/AC:H/PR:N/UI:N/S:U/C:H/I:H/A:N

---

### Finding 7: Timing Attack

- **Severity:** Medium (CVSS 4.7)
- **Category:** Weak Cryptography
- **CWE:** CWE-208
- **Reachability:** LIKELY
- **File:** `/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/tests/test_sexp.py:262`
- **Language:** python

**What the code does:**
```
if isinstance(n, list) and n and n[0] == keyword:
```

**Vulnerability:**
A security vulnerability (timing_attack) exists at test_sexp.py:262. Potential timing attack vulnerability. Exploitation could lead to Weak Cryptography. Note: some sanitization was detected (assertion), but it may be insufficient or bypassable.

**Code Path:**
`/Users/johnbetts/Dropbox/Source/kicad-ai-workflow/tests/test_sexp.py:329 -> /Users/johnbetts/Dropbox/Source/kicad-ai-workflow/tests/test_sexp.py:262`

**Proof of Concept:**
The vulnerability at test_sexp.py:262 (timing_attack) could be exploited by crafting input that reaches the vulnerable code path without adequate sanitization.

**Recommended Fix (AUTO_FIX):**

Before:
```
if isinstance(n, list) and n and n[0] == keyword:
```

After:
```
# TODO: Fix timing_attack — Use hmac.compare_digest() or crypto.timingSafeEqual() for constant-time comparison
```

**Why this fix works:** Use hmac.compare_digest() or crypto.timingSafeEqual() for constant-time comparison

**Regression test:**
```python
def test_timing_attack_fixed():
    # Verify the timing_attack vulnerability is fixed
    # Add specific test for /Users/johnbetts/Dropbox/Source/kicad-ai-workflow/tests/test_sexp.py:262
    pass  # TODO: implement regression test

```

**CVSS Vector:**
AV:N/AC:H/PR:N/UI:N/S:U/C:L/I:L/A:N

---

## Attack Surface Map

### Entry Points (11 found)

- `test_board_templates.py:167` — def test_detect_template_none_input() -> None:
- `test_subcircuits.py:191` — """usb_c_input() returns connector + 2 CC resistors = 3 components."""
- `test_subcircuits.py:192` — result = usb_c_input("J1", "R_CC1", "R_CC2")
- `test_subcircuits.py:198` — result = usb_c_input("J1", "R_CC1", "R_CC2")
- `test_subcircuits.py:210` — result = usb_c_input("J_USB", "R5", "R6")
- `test_sexp.py:329` — def test_parse_raises_on_empty_input() -> None:
- `regenerate_smd0603.py:118` — if __name__ == "__main__":
- `generate_adc_schematic.py:372` — if __name__ == "__main__":
- `subcircuits.py:1030` — def usb_c_input(
- `main.py:498` — if __name__ == "__main__":
- `main.py:12` — parser = argparse.ArgumentParser(

### Trust Boundaries

- **User Input:** HTTP request parameters, form data, headers, cookies, file uploads
- **Internal Processing:** Business logic, data transformation, validation
- **External Systems:** Databases, APIs, file system, email, cloud services
- **Output Sinks:** HTTP responses, template rendering, file writes, logs

## Reachability Analysis

| Classification | Count | Percentage |
|----------------|-------|------------|
| CONFIRMED | 0 | 0.0% |
| LIKELY | 7 | 28.0% |
| UNLIKELY | 18 | 72.0% |
| FALSE_POSITIVE | 0 | 0.0% |

## Recommendations

Prioritized by risk (highest first):

1. **[High]** Fix code execution in `test_placement_visual.py:41` — Avoid eval/exec; use safe alternatives (AST, JSON.parse, etc.)
2. **[High]** Fix sql injection in `design_state.py:86` — Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it 
3. **[High]** Fix sql injection in `agents_cmd.py:201` — Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it 
4. **[High]** Fix sql injection in `agents_cmd.py:295` — Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it 
5. **[High]** Fix sql injection in `jlcpcb_db.py:260` — Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it 
6. **[High]** Fix sql injection in `jlcpcb_db.py:292` — Use parameterized queries to prevent SQL injection. The database driver handles escaping, making it 
7. **[Medium]** Fix timing attack in `test_sexp.py:262` — Use hmac.compare_digest() or crypto.timingSafeEqual() for constant-time comparison

---
*Generated by Code Auditor on 2026-03-22 23:54:36 UTC*